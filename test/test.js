
describe('Output the configured JSON object', () => {
    it('should return default configuration list', () => {
        // 默认配置列表
    });

    it('should return custom configuration list', () => {
        // 自定义配置列表
    });
});

describe('Generate the information object to compile to md', () => {
    // 输出待编译为 md 的 json 对象
});
